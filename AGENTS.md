# Relight项目长期约定

## 项目边界

- 本项目只接收一个图片目录，完成VL选图、重打光指令生成和单轮图生图。
- 不增加数据集下载、尺寸/水印质量筛选、人像子集筛选或四轮编辑业务。
- 输入图片只读，不移动、不覆盖、不重编码；交付结果只写入本项目的`output/`。

## 流水线调度

- 必须按单张图片连续推进完整流程；一张完成后立即补入下一张候选。
- 禁止使用批次屏障，也不得增加`*_QUEUE_BATCH_SIZE`控制业务调度。
- 禁止为全库一次创建数万个协程；使用有界工作池或`FIRST_COMPLETED`流式补位。
- 外部API并发由VL、上传、提交、在途生成、轮询、下载和编码各自参数控制。
- 已持久化业务ID或任务ID的任务必须优先恢复，不得重复创建付费任务。

## 稳定性与安全

- 全局渠道、模型、鉴权或账户硬错误触发熔断；下次`--resume`从SQLite继续。
- 长时间运行必须显示可关闭的进度条，关键配置、业务逻辑和异常处理必须有注释。
- API Key、OSS Secret和临时签名URL不得写入源码、日志、SQLite或结果JSON。
- OSS默认关闭；开启时本地交付结果仍必须完整保留。
- 新功能必须通过可配置模块扩展，不得把模型、路径或请求字段写死在业务循环中。
- 离线测试不得调用真实Qwen、ToAPIs或产生付费任务，除非用户明确授权。

